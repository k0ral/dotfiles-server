#!/bin/bash
#
# /etc/rc.local: Local multi-user startup script.
#

IRC_SCREENS=`sudo -u koral screen -ls | grep IRC | wc -l`
TTRSS_SCREENS=`screen -ls | grep TTRSS | wc -l`

[ "$IRC_SCREENS" -eq 0 ]   && sudo -u koral screen -S IRC -d -m weechat-curses
[ "$TTRSS_SCREENS" -eq 0 ] && screen -S TTRSS -d -m /home/koral/script/update_ttrss.sh
